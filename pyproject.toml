[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "delta-spark>=4.1.0",
    "numpy>=2.4.4",
    "psutil>=7.2.2",
    "pyarrow>=23.0.1",
]
